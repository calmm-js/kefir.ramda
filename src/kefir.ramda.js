// THIS FILE IS GENERATED

import * as R from 'ramda'
import {liftRec} from 'kefir.combines'

export const F = liftRec(R.F)
export const T = liftRec(R.T)
export const __ = liftRec(R.__)
export const add = liftRec(R.add)
export const addIndex = liftRec(R.addIndex)
export const adjust = liftRec(R.adjust)
export const all = liftRec(R.all)
export const allPass = liftRec(R.allPass)
export const always = liftRec(R.always)
export const and = liftRec(R.and)
export const any = liftRec(R.any)
export const anyPass = liftRec(R.anyPass)
export const ap = liftRec(R.ap)
export const aperture = liftRec(R.aperture)
export const append = liftRec(R.append)
export const apply = liftRec(R.apply)
export const applySpec = liftRec(R.applySpec)
export const applyTo = liftRec(R.applyTo)
export const ascend = liftRec(R.ascend)
export const assoc = liftRec(R.assoc)
export const assocPath = liftRec(R.assocPath)
export const binary = liftRec(R.binary)
export const bind = liftRec(R.bind)
export const both = liftRec(R.both)
export const call = liftRec(R.call)
export const chain = liftRec(R.chain)
export const clamp = liftRec(R.clamp)
export const clone = liftRec(R.clone)
export const comparator = liftRec(R.comparator)
export const complement = liftRec(R.complement)
export const compose = liftRec(R.compose)
export const composeK = liftRec(R.composeK)
export const composeP = liftRec(R.composeP)
export const concat = liftRec(R.concat)
export const cond = liftRec(R.cond)
export const construct = liftRec(R.construct)
export const constructN = liftRec(R.constructN)
export const contains = liftRec(R.contains)
export const converge = liftRec(R.converge)
export const countBy = liftRec(R.countBy)
export const curry = liftRec(R.curry)
export const curryN = liftRec(R.curryN)
export const dec = liftRec(R.dec)
export const defaultTo = liftRec(R.defaultTo)
export const descend = liftRec(R.descend)
export const difference = liftRec(R.difference)
export const differenceWith = liftRec(R.differenceWith)
export const dissoc = liftRec(R.dissoc)
export const dissocPath = liftRec(R.dissocPath)
export const divide = liftRec(R.divide)
export const drop = liftRec(R.drop)
export const dropLast = liftRec(R.dropLast)
export const dropLastWhile = liftRec(R.dropLastWhile)
export const dropRepeats = liftRec(R.dropRepeats)
export const dropRepeatsWith = liftRec(R.dropRepeatsWith)
export const dropWhile = liftRec(R.dropWhile)
export const either = liftRec(R.either)
export const empty = liftRec(R.empty)
export const endsWith = liftRec(R.endsWith)
export const eqBy = liftRec(R.eqBy)
export const eqProps = liftRec(R.eqProps)
export const equals = liftRec(R.equals)
export const evolve = liftRec(R.evolve)
export const filter = liftRec(R.filter)
export const find = liftRec(R.find)
export const findIndex = liftRec(R.findIndex)
export const findLast = liftRec(R.findLast)
export const findLastIndex = liftRec(R.findLastIndex)
export const flatten = liftRec(R.flatten)
export const flip = liftRec(R.flip)
export const forEach = liftRec(R.forEach)
export const forEachObjIndexed = liftRec(R.forEachObjIndexed)
export const fromPairs = liftRec(R.fromPairs)
export const groupBy = liftRec(R.groupBy)
export const groupWith = liftRec(R.groupWith)
export const gt = liftRec(R.gt)
export const gte = liftRec(R.gte)
export const has = liftRec(R.has)
export const hasIn = liftRec(R.hasIn)
export const head = liftRec(R.head)
export const identical = liftRec(R.identical)
export const identity = liftRec(R.identity)
export const ifElse = liftRec(R.ifElse)
export const inc = liftRec(R.inc)
export const indexBy = liftRec(R.indexBy)
export const indexOf = liftRec(R.indexOf)
export const init = liftRec(R.init)
export const innerJoin = liftRec(R.innerJoin)
export const insert = liftRec(R.insert)
export const insertAll = liftRec(R.insertAll)
export const intersection = liftRec(R.intersection)
export const intersperse = liftRec(R.intersperse)
export const into = liftRec(R.into)
export const invert = liftRec(R.invert)
export const invertObj = liftRec(R.invertObj)
export const invoker = liftRec(R.invoker)
export const is = liftRec(R.is)
export const isEmpty = liftRec(R.isEmpty)
export const isNil = liftRec(R.isNil)
export const join = liftRec(R.join)
export const juxt = liftRec(R.juxt)
export const keys = liftRec(R.keys)
export const keysIn = liftRec(R.keysIn)
export const last = liftRec(R.last)
export const lastIndexOf = liftRec(R.lastIndexOf)
export const length = liftRec(R.length)
export const lens = liftRec(R.lens)
export const lensIndex = liftRec(R.lensIndex)
export const lensPath = liftRec(R.lensPath)
export const lensProp = liftRec(R.lensProp)
export const lift = liftRec(R.lift)
export const liftN = liftRec(R.liftN)
export const lt = liftRec(R.lt)
export const lte = liftRec(R.lte)
export const map = liftRec(R.map)
export const mapAccum = liftRec(R.mapAccum)
export const mapAccumRight = liftRec(R.mapAccumRight)
export const mapObjIndexed = liftRec(R.mapObjIndexed)
export const match = liftRec(R.match)
export const mathMod = liftRec(R.mathMod)
export const max = liftRec(R.max)
export const maxBy = liftRec(R.maxBy)
export const mean = liftRec(R.mean)
export const median = liftRec(R.median)
export const memoize = liftRec(R.memoize)
export const memoizeWith = liftRec(R.memoizeWith)
export const merge = liftRec(R.merge)
export const mergeAll = liftRec(R.mergeAll)
export const mergeDeepLeft = liftRec(R.mergeDeepLeft)
export const mergeDeepRight = liftRec(R.mergeDeepRight)
export const mergeDeepWith = liftRec(R.mergeDeepWith)
export const mergeDeepWithKey = liftRec(R.mergeDeepWithKey)
export const mergeWith = liftRec(R.mergeWith)
export const mergeWithKey = liftRec(R.mergeWithKey)
export const min = liftRec(R.min)
export const minBy = liftRec(R.minBy)
export const modulo = liftRec(R.modulo)
export const multiply = liftRec(R.multiply)
export const nAry = liftRec(R.nAry)
export const negate = liftRec(R.negate)
export const none = liftRec(R.none)
export const not = liftRec(R.not)
export const nth = liftRec(R.nth)
export const nthArg = liftRec(R.nthArg)
export const o = liftRec(R.o)
export const objOf = liftRec(R.objOf)
export const of = liftRec(R.of)
export const omit = liftRec(R.omit)
export const once = liftRec(R.once)
export const or = liftRec(R.or)
export const over = liftRec(R.over)
export const pair = liftRec(R.pair)
export const partial = liftRec(R.partial)
export const partialRight = liftRec(R.partialRight)
export const partition = liftRec(R.partition)
export const path = liftRec(R.path)
export const pathEq = liftRec(R.pathEq)
export const pathOr = liftRec(R.pathOr)
export const pathSatisfies = liftRec(R.pathSatisfies)
export const pick = liftRec(R.pick)
export const pickAll = liftRec(R.pickAll)
export const pickBy = liftRec(R.pickBy)
export const pipe = liftRec(R.pipe)
export const pipeK = liftRec(R.pipeK)
export const pipeP = liftRec(R.pipeP)
export const pluck = liftRec(R.pluck)
export const prepend = liftRec(R.prepend)
export const product = liftRec(R.product)
export const project = liftRec(R.project)
export const prop = liftRec(R.prop)
export const propEq = liftRec(R.propEq)
export const propIs = liftRec(R.propIs)
export const propOr = liftRec(R.propOr)
export const propSatisfies = liftRec(R.propSatisfies)
export const props = liftRec(R.props)
export const range = liftRec(R.range)
export const reduce = liftRec(R.reduce)
export const reduceBy = liftRec(R.reduceBy)
export const reduceRight = liftRec(R.reduceRight)
export const reduceWhile = liftRec(R.reduceWhile)
export const reduced = liftRec(R.reduced)
export const reject = liftRec(R.reject)
export const remove = liftRec(R.remove)
export const repeat = liftRec(R.repeat)
export const replace = liftRec(R.replace)
export const reverse = liftRec(R.reverse)
export const scan = liftRec(R.scan)
export const sequence = liftRec(R.sequence)
export const set = liftRec(R.set)
export const slice = liftRec(R.slice)
export const sort = liftRec(R.sort)
export const sortBy = liftRec(R.sortBy)
export const sortWith = liftRec(R.sortWith)
export const split = liftRec(R.split)
export const splitAt = liftRec(R.splitAt)
export const splitEvery = liftRec(R.splitEvery)
export const splitWhen = liftRec(R.splitWhen)
export const startsWith = liftRec(R.startsWith)
export const subtract = liftRec(R.subtract)
export const sum = liftRec(R.sum)
export const symmetricDifference = liftRec(R.symmetricDifference)
export const symmetricDifferenceWith = liftRec(R.symmetricDifferenceWith)
export const tail = liftRec(R.tail)
export const take = liftRec(R.take)
export const takeLast = liftRec(R.takeLast)
export const takeLastWhile = liftRec(R.takeLastWhile)
export const takeWhile = liftRec(R.takeWhile)
export const tap = liftRec(R.tap)
export const test = liftRec(R.test)
export const times = liftRec(R.times)
export const toLower = liftRec(R.toLower)
export const toPairs = liftRec(R.toPairs)
export const toPairsIn = liftRec(R.toPairsIn)
export const toString = liftRec(R.toString)
export const toUpper = liftRec(R.toUpper)
export const transduce = liftRec(R.transduce)
export const transpose = liftRec(R.transpose)
export const traverse = liftRec(R.traverse)
export const trim = liftRec(R.trim)
export const tryCatch = liftRec(R.tryCatch)
export const type = liftRec(R.type)
export const unapply = liftRec(R.unapply)
export const unary = liftRec(R.unary)
export const uncurryN = liftRec(R.uncurryN)
export const unfold = liftRec(R.unfold)
export const union = liftRec(R.union)
export const unionWith = liftRec(R.unionWith)
export const uniq = liftRec(R.uniq)
export const uniqBy = liftRec(R.uniqBy)
export const uniqWith = liftRec(R.uniqWith)
export const unless = liftRec(R.unless)
export const unnest = liftRec(R.unnest)
export const until = liftRec(R.until)
export const update = liftRec(R.update)
export const useWith = liftRec(R.useWith)
export const values = liftRec(R.values)
export const valuesIn = liftRec(R.valuesIn)
export const view = liftRec(R.view)
export const when = liftRec(R.when)
export const where = liftRec(R.where)
export const whereEq = liftRec(R.whereEq)
export const without = liftRec(R.without)
export const xprod = liftRec(R.xprod)
export const zip = liftRec(R.zip)
export const zipObj = liftRec(R.zipObj)
export const zipWith = liftRec(R.zipWith)
